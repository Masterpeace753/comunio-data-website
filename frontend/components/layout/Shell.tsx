"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { BarChart3, Menu, Search, ShieldCheck, Users, X, ArrowLeftRight } from "lucide-react";
import { useState } from "react";
const links = [{ href: "/", label: "Dashboard", icon: BarChart3 }, { href: "/spieler", label: "Spieler", icon: Users }, { href: "/teams", label: "Teams", icon: ShieldCheck }, { href: "/transfermarkt", label: "Transfermarkt", icon: ArrowLeftRight }];
export function Shell({ children }: { children: React.ReactNode }) { const path = usePathname(); const [open, setOpen] = useState(false); return <div className="min-h-screen md:flex"><aside className={`fixed inset-y-0 left-0 z-30 w-72 border-r border-line bg-surface/95 p-6 backdrop-blur md:sticky md:block md:h-screen ${open ? "block" : "hidden"}`}><div className="mb-12 flex items-center justify-between"><Link href="/" className="text-xl font-bold tracking-tight" onClick={() => setOpen(false)}><span className="text-accent">COMUNIO</span> <span className="text-violet">DATA</span></Link><button className="button md:hidden" aria-label="Menü schließen" onClick={() => setOpen(false)}><X size={18}/></button></div><nav className="space-y-2">{links.map(({ href, label, icon: Icon }) => <Link key={href} href={href} onClick={() => setOpen(false)} className={`flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold transition ${path === href || (href !== "/" && path.startsWith(href)) ? "bg-accent/15 text-accent" : "text-secondary hover:bg-elevated hover:text-ink"}`}><Icon size={18}/>{label}</Link>)}</nav><div className="absolute bottom-6 left-6 right-6 rounded-xl border border-line bg-canvas p-4"><p className="eyebrow">Datenmodus</p><p className="mt-2 flex items-center gap-2 text-sm text-secondary"><span className="h-2 w-2 rounded-full bg-success"/> Read-only API</p></div></aside><main className="min-w-0 flex-1"><header className="sticky top-0 z-20 flex h-20 items-center justify-between border-b border-line bg-canvas/85 px-5 backdrop-blur md:px-10"><button className="button md:hidden" aria-label="Menü öffnen" onClick={() => setOpen(true)}><Menu size={19}/></button><Link href="/spieler" className="hidden items-center gap-2 text-sm text-secondary md:flex"><Search size={16}/> Globale Spielersuche</Link><div className="ml-auto flex items-center gap-3 text-right"><p className="hidden text-xs text-secondary sm:block">Zuletzt aktualisiert am <strong className="text-ink">--</strong></p><div className="h-9 w-9 rounded-full border border-line bg-elevated p-2 text-accent"><BarChart3 size={18}/></div></div></header><div className="mx-auto max-w-[1500px] p-5 md:p-10">{children}</div></main></div>; }