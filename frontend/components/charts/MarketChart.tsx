"use client";
import { Area, AreaChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { euro, dateLabel } from "@/lib/format";
import type { MarketValuePoint } from "@/lib/api/types";
export function MarketChart({ data }: { data: MarketValuePoint[] }) { const chart = [...data].reverse(); return <div className="h-80 w-full"><ResponsiveContainer><AreaChart data={chart} margin={{ top: 10, right: 10, left: 0, bottom: 0 }}><defs><linearGradient id="market-fill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#4C7DFF" stopOpacity={.35}/><stop offset="100%" stopColor="#4C7DFF" stopOpacity={0}/></linearGradient></defs><XAxis dataKey="snapshot_date" tickFormatter={dateLabel} tick={{ fill: "#5D6491", fontSize: 11 }} axisLine={false} tickLine={false}/><YAxis tickFormatter={(v) => `${Math.round(v / 1000)}k`} tick={{ fill: "#5D6491", fontSize: 11 }} axisLine={false} tickLine={false}/><Tooltip contentStyle={{ background: "#171B33", border: "1px solid #232848", borderRadius: 12 }} labelFormatter={(value) => dateLabel(typeof value === "string" ? value : null)} formatter={(value) => [euro(Number(value)), "Marktwert"]}/><Area type="monotone" dataKey="value_eur" stroke="#6E9BFF" strokeWidth={2} fill="url(#market-fill)"/></AreaChart></ResponsiveContainer></div>; }