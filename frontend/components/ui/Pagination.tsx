import Link from "next/link";
export function Pagination({ total, offset, limit, base, query = "" }: { total: number; offset: number; limit: number; base: string; query?: string }) { const previous = Math.max(0, offset - limit); const next = offset + limit; return <div className="flex items-center justify-between border-t border-line px-4 py-4 text-sm text-secondary"><span>Zeige {total === 0 ? 0 : offset + 1}-{Math.min(offset + limit, total)} von {total}</span><div className="flex gap-2"><Link aria-disabled={offset === 0} className={`button ${offset === 0 ? "pointer-events-none opacity-40" : ""}`} href={`${base}?${query}${query ? "&" : ""}offset=${previous}`}>Zurück</Link><Link aria-disabled={next >= total} className={`button ${next >= total ? "pointer-events-none opacity-40" : ""}`} href={`${base}?${query}${query ? "&" : ""}offset=${next}`}>Weiter</Link></div></div>; }